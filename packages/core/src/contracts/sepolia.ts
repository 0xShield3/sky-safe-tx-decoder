/**
 * Well-known contracts on Sepolia testnet (chainId 11155111).
 * Add a new contract by appending an entry to the array.
 */

import type { NetworkContract } from './types.js'

export const CONTRACTS: NetworkContract[] = []
